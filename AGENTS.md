# 開発
## TODO.md
`TODO.md`を確認する

作業が完了したら、対応する `TODO.md` のチェックボックスを都度更新する（`- [ ]` → `- [x]`）。
一部だけ実装した場合はチェックを付けず、実装状況が分かる補足を項目の下に追記する。

# 実装のルール
以下のルールに従い、知識を混ぜてはいけない。frontendがbackendの事情を知っているコードやコメントを書いてはいけない(スキーマは例外)
- frontendはfrontendとsharedのコードしか知らない
- backendはbackendとsharedのコードしか知らない

## コメント
基本的にコードコメントは書かない、変数名、関数名からわからない、複雑な場合のみ特別に許可される。  
mastodon, activity pubのコメントは詳細に記述する
コメントを書く前に、その情報が無ければコードの変更時に誤った判断をするかを確認する。  
必要な場合は、コードでは表現できない制約を、その制約が成立する最小のスコープに書く。  
コードと同じ内容や変更の経緯は、コードと git の履歴を正とする。  

## 基本的に禁止な事項
- Global public関数
  - 慣習でGlobalなものは許可(例: Composeのremember*)

# ルール
日本語で記述するもの
- コードコメント
- PR文
- コミットコメント
- テスト名
- コードレビューのコメント

コミットを実装の意味単位で行う。まとめてコミットしてはいけない。  

マージコミットのメッセージは git の既定の文面を使う。衝突を解消したあとも `-m` で書き直さず、`git commit` だけにする。

# PRのルール
PR作成時にマージ先の最新が取り込まれているか確認する。  
レビューには返信する。対応したらコミットhashを添えて。  
レビュースレッドは勝手に Resolve しない。Resolve するのは利用者から明示されたときだけ。

## レビュー対応について
- ホビープロジェクトなので、セキュリティ周り以外、要件を満たさない以外の細かい対応は不要。BotやAIがユーザーに成り代わって投稿したもの以外の、ユーザーが直接コメントした場合は必ず対応する。  

## ドキュメント
Markdown で `**` による太字装飾を使わない。強調したい場合は文章の書き方で表現する。  
KDocは`/** */`で書かず、必ず改行する。  
ドキュメントを更新する前に、誰がどの作業でその情報を必要とするかを決める。
対象となる読み手と作業が無ければ更新しない。情報はその作業に必要な抽象度に留め、正となる場所を一つにする。
実装そのものはコード、変更の経緯は git の履歴を正とし、ドキュメントと同期させる情報を増やさない。

- `README.md` は利用者がビルド、設定、運用するために必要な情報だけを書く
- `docs/architecture.md` はモジュール境界や全体に影響する設計判断だけを書く。局所的な実装方法は書かない
- `TODO.md` は今後の作業、順序、完了状況だけを書く。完了した実装の詳細は書かない

# モデルごとのルール
## Cursor系(Grok, Composer)
PRを提出したらCIやコメントの結果を購読して対応する。  


コミットしたら最新のPRの内容をfetchしてから更新を行う
