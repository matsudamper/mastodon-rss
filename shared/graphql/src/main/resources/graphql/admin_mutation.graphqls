type AdminMutation {
    "通るとセッションの Cookie が返る"
    login(password: String!): AdminLoginResult!

    "サーバー側のセッションも消える"
    logout: AdminLogoutResult!
}

type AdminLogoutResult {
    isSuccess: Boolean!
}

type AdminLoginResult {
    isSuccess: Boolean!
    failure: AdminLoginFailure
}

enum AdminLoginFailure {
    WRONG_PASSWORD

    "ADMIN_PASSWORD_HASH が未設定"
    NOT_CONFIGURED
}
