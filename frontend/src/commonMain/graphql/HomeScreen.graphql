query HomeScreen($cursor: String, $limit: Int!) {
    accounts(cursor: $cursor, limit: $limit) {
        nodes {
            id
            username
            acct
            actorUrl
            displayName
            summary
        }
        pageInfo {
            hasMore
            nextCursor
        }
    }
}
