query HomeScreen($query: TimelineQuery!) {
    timeline(query: $query) {
        nodes {
            ...AccountNoteFields
            account {
                id
                username
                acct
                displayName
                iconUrl
            }
        }
        pageInfo {
            hasMore
            nextCursor
        }
    }
}
