query AccountFollowersQuery($query: AccountFollowersQuery!) {
    followers(query: $query) {
        nodes {
            url
            acct
            displayName
            iconUrl
        }
        pageInfo {
            hasMore
            nextCursor
        }
    }
}
