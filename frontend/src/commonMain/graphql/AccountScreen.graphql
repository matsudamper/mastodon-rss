query AccountScreen($username: String!) {
    account(username: $username) {
        id
        username
        acct
        actorUrl
        displayName
        summary
        followerCount
        noteCount
        feed {
            url
            siteUrl
        }
    }
}
