query AccountScreen($username: String!, $query: AccountNotesQuery!) {
    account(username: $username) {
        id
        username
        acct
        actorUrl
    }
    notes(query: $query) {
        nodes {
            ...AccountNoteFields
        }
        pageInfo {
            hasMore
            nextCursor
        }
    }
}
