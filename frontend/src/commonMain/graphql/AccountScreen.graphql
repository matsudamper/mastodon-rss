query AccountScreen($username: String!, $query: AccountNotesQuery!) {
    account(username: $username) {
        username
        acct
        actorUrl
    }
    notes(query: $query) {
        nodes {
            ...AccountNoteFields
        }
        pageInfo {
            hasMore
            nextCursor
        }
    }
}
