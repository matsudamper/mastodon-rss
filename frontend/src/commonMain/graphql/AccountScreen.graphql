query AccountScreen($username: String!) {
    account(username: $username) {
        id
        username
        acct
        actorUrl
        displayName
        summary
        iconUrl
        headerUrl
        followerCount
        noteCount
        feed {
            url
            siteUrl
        }
    }
}
