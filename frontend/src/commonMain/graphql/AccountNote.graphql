query AccountNote($username: String!, $id: PublicNoteId!) {
    note(username: $username, id: $id) {
        ...AccountNoteFields
        linkUrls
        account {
            id
            username
            acct
            displayName
            iconUrl
        }
    }
}
